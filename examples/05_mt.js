// scale(c, 'major')

// scale(c, 'major').filter(only(1,3,5)).map(play).forEach(log)

// scale(c, 'major').filter(only(4,6,1)).map(play).forEach(log)

// scale(c, 'major').filter(only(5,7,2)).map(play).forEach(log)

// var progression = [2, 2, 1, 2, 2, 2, 1]

// sequence('c', progression).forEach((note,i) => setTimeout(() => [note].map(play).forEach(piano), i*500))

// progression.unshift(progression.pop())
