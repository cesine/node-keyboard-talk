// scale(c, 'major')

// scale(c, 'major').filter(only(1,3,5)).forEach(play)

// scale(c, 'major').filter(only(4,6,1)).forEach(play)

// scale(c, 'major').filter(only(5,7,2)).forEach(play)

// var progression = [2, 2, 1, 2, 2, 2, 1]

// sequence('c', progression).forEach((note,i) => setTimeout(() => play(note), i*500))

// progression.unshift(progression.pop())
// progression.unshift(progression.pop())

// sequence('c', progression).forEach((note,i) => setTimeout(() => play(note), i*500))
